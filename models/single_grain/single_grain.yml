version: 2

models:
  - name: single_grain
    description: 'Each row in this table represents one prediction per model run date per single x variable'
    columns:
      - name: account_id
        description: The ID of the ad account.
